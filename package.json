{
  "name": "ReactNativeIAmPort",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node node_modules/react-native/local-cli/cli.js start",
    "test": "jest",
    "clean": "watchman watch-del-all && rm -rf $TMPDIR/react* && npm cache clean --force && rm -rf node_modules && yarn install"
  },
  "dependencies": {
    "react": "16.0.0-alpha.12",
    "react-native": "^0.45.1",
    "react-native-iamport": "git+https://github.com/jeongjuwon/react-native-iamport.git",
    "react-native-router-flux": "^3.40.0"
  },
  "devDependencies": {
    "babel-jest": "19.0.0",
    "babel-preset-react-native": "1.9.2",
    "jest": "20.0.4",
    "react-test-renderer": "16.0.0-alpha.12"
  },
  "jest": {
    "preset": "react-native"
  }
}
